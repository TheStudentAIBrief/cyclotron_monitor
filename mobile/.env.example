# Copy to .env and set your API URL before running.
# For local dev (same WiFi network): use your machine's local IP.
# For production: use the Render.com URL.
EXPO_PUBLIC_API_URL=http://192.168.1.X:8000
